import React from 'react';
import PropTypes from 'prop-types';

const QuestionAnswer = (props)=>{
    return (
      <div> here is the QuestionAnswer component.</div>
    )
  }

export default QuestionAnswer;